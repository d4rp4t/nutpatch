///
/// HybridOutputCreatorSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeOutputData` to properly resolve imports.
namespace margelo::nitro::nutpatch { struct NativeOutputData; }
// Forward declaration of `NitroP2PKOptions` to properly resolve imports.
namespace margelo::nitro::nutpatch { struct NitroP2PKOptions; }
// Forward declaration of `Keyset` to properly resolve imports.
namespace margelo::nitro::nutpatch { struct Keyset; }

#include "NativeOutputData.hpp"
#include <vector>
#include "NitroP2PKOptions.hpp"
#include "Keyset.hpp"
#include <optional>
#include <string>
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::nutpatch {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `OutputCreator`
   * Inherit this class to create instances of `HybridOutputCreatorSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridOutputCreator: public HybridOutputCreatorSpec {
   * public:
   *   HybridOutputCreator(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridOutputCreatorSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridOutputCreatorSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridOutputCreatorSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::vector<NativeOutputData> createP2PKData(const NitroP2PKOptions& p2pk, uint64_t amount, const Keyset& keyset, const std::optional<std::vector<uint64_t>>& customSplit) = 0;
      virtual NativeOutputData createSingleP2PKData(const NitroP2PKOptions& p2pk, uint64_t amount, const std::string& keysetId) = 0;
      virtual std::vector<NativeOutputData> createRandomData(uint64_t amount, const Keyset& keyset, const std::optional<std::vector<uint64_t>>& customSplit) = 0;
      virtual NativeOutputData createSingleRandomData(uint64_t amount, const std::string& keysetId) = 0;
      virtual std::vector<NativeOutputData> createDeterministicData(uint64_t amount, const std::shared_ptr<ArrayBuffer>& seed, uint64_t counter, const Keyset& keyset, const std::optional<std::vector<uint64_t>>& customSplit) = 0;
      virtual NativeOutputData createSingleDeterministicData(uint64_t amount, const std::shared_ptr<ArrayBuffer>& seed, uint64_t counter, const std::string& keysetId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "OutputCreator";
  };

} // namespace margelo::nitro::nutpatch
